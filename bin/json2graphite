#! /usr/bin/env ruby

require 'json'
require 'timeout'
require 'pp'

def run
  body = ''
  #Timeout.timeout(10) do
    body += STDIN.read
    hash = JSON.parse(body)
    data = to_graphite(hash)
  #end
end

run()

